services:
  postgres:
    image: postgres:18
    container_name: pokedex-db

    environment:
      POSTGRES_DB: pokedex
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres

    ports:
      - "5432:5432"
